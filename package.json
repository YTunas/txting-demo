{
  "name": "txting-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "cd txting-app && npm install && cd ../server && npm install",
    "build": "cd txting-app && npm install && ng build --configuration production",
    "start": "cd server && npm install && node server.js",
    "deploy": "npm run install-all && npm run build"
  },
  "engines": {
    "node": "~18.19.0",
    "npm": "~10.2.3"
  }
}