{
  "name": "txting-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "cd server && npm install && cd ../txting-app && npm install",
    "build": "cd txting-app && npm run build",
    "start": "cd server && NODE_ENV=production node server.js",
    "postinstall": "npm run install-all && npm run build"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}